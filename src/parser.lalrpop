use std::str::FromStr;
use crate::lexer;

grammar;

pub Term: i32 = {
    "flip" => 0,
//    "(" <t:Term> ")" => 0,
};

//Num: i32 = <s:r"[0-9]+"> => i32::from_str(s).unwrap();

extern {
    type Location = lexer::Loc;
    type Error = lexer::LexicalError;

    enum lexer::Tok {
        "flip" => lexer::Tok::Flip,
    }
}
